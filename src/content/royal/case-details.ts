import type {
  RoyalCaseDetailRecord,
} from "./case-detail.ts";

/**
 * Royal-specific Case Detail catalog.
 *
 * Intentionally empty until a Case has controlled,
 * verified and publication-authorized narrative content.
 *
 * Test fixtures must remain under tests/.
 */
export const royalCaseDetails:
  readonly RoyalCaseDetailRecord[] = [];
