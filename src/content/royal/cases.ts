import type {
  Evidence,
  SiteCase,
} from "../../domains/cases/index.ts";

/**
 * Royal-specific Case/Evidence catalog.
 *
 * Intentionally empty until controlled source material is
 * verified and explicitly authorized for publication.
 *
 * Legacy media, landing-page copy and testimonial fallbacks
 * must not be promoted into this catalog implicitly.
 */
export const royalCases: readonly SiteCase[] = [];

export const royalEvidence: readonly Evidence[] = [];
