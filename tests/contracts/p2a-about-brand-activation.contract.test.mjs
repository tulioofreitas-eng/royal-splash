import test from"node:test";import assert from"node:assert/strict";import{readFile}from"node:fs/promises";const read=p=>readFile(p,"utf8");
test("A Royal retains Brand activation and SC-R14 institutional boundaries",async()=>{const s=await read("src/pages/sobre.astro");assert.match(s,/visualMode="brand"/);assert.match(s,/<h1[^>]*>Engenharia de água/);assert.doesNotMatch(s,/MTM Group/);assert.match(s,/href="\/projetos"/);assert.match(s,/wa\.me\/5521982590643/);assert.doesNotMatch(s,/20\+|500\+|100%|Santa Catarina|certificaç(?:ão|ões) [^.]*Royal/i)});
test("A Royal uses the accepted experience layer without provider coupling",async()=>{const s=await read("src/pages/sobre.astro");for(const token of["site-primitive-page","experience-entry","experience-section","experience-grid","experience-conversion","site-primitive-action--primary"])assert.ok(s.includes(token),token);assert.doesNotMatch(s,/Supabase|Atlas|Formspree|GHL|StructuredIntake/)});
