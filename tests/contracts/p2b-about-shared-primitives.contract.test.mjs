import test from"node:test";import assert from"node:assert/strict";import{readFile}from"node:fs/promises";
test("A Royal consumes shared SC-R14 experience composition",async()=>{const s=await readFile("src/pages/sobre.astro","utf8");for(const x of["complete-experience.css","experience-entry","experience-media","experience-grid","experience-card","experience-conversion"])assert.ok(s.includes(x),x);assert.match(s,/bodyClass="site-primitive-page experience-page"/)});
test("A Royal preserves verifiable content and direct conversion",async()=>{const s=await readFile("src/pages/sobre.astro","utf8");assert.doesNotMatch(s,/MTM Group/);assert.match(s,/href="\/projetos"/);assert.match(s,/wa\.me\/5521982590643/);assert.match(s,/noopener noreferrer/);assert.doesNotMatch(s,/20\+|500\+|Santa Catarina|equipe própria|<form/i)});
